using System;
namespace CmisSync.Lib.Sync.SynchronizeMachine
{
    /*
     * Create semi-finished products of synchronized triplets from CmisRepo's
     * local / db / remote items.
     */
    public class CmisRepoProcessor
    {
        public CmisRepoProcessor ()
        {
        }
    }
}
