using System;

namespace CmisSync.Lib.Sync.SynchronizeMachine
{
    /*
     * Semi-finished products processor for synchronized triplets.
     * After Local/DB/Remote fields are properly filled, a triplet
     * would be pushed to synctriplet processer
     * 
     * 1st sync() : 
     *   First pass : create LS _ _ from local files
     */
    public class SFTripletProcessor
    {
        public SFTripletProcessor()
        {
        }
    }
}
